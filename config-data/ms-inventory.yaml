server:
  port: ${PORT:0}

spring:
  application:
    name: ms-inventory
  datasource:
    url: jdbc:mysql://mysql:3306/toolrent_inventory?allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true
    username: root
    password: 19Luna!Cody03
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        # dialect: org.hibernate.dialect.MySQL8Dialect # Deprecated in Hibernate 6, let it auto-detect
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://eureka-service:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
